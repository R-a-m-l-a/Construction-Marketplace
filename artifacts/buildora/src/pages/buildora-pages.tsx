import { useMemo, useState, type ChangeEvent, type FormEvent, type ReactNode } from "react";
import { Link, useLocation } from "wouter";
import { ArrowDownRight, ArrowRight, BadgeCheck, Calculator, Check, ChevronDown, CircleHelp, Compass, Hammer, MapPin, MessageSquareText, Search, ShieldCheck, Sparkles, Star, Store, Users, Wrench } from "lucide-react";
import { useGetDiscoveryStats, useListCategories, useListFeaturedProfessionals, useListFeaturedProducts } from "@workspace/api-client-react";
import type { Category, ProfessionalPreview, ProductPreview } from "@workspace/api-client-react";
import { DemoTag, LoadingCards, PageIntro, QueryError, SectionLabel, Shell } from "@/components/buildora-shell";

const fallbackCategories: Category[] = [
  { id: "cement", name: "Cement & concrete", group: "Materials", icon: "layers", description: "The foundations, literally." },
  { id: "finishes", name: "Finishes & surfaces", group: "Materials", icon: "grid", description: "Make the everyday feel right." },
  { id: "design", name: "Design & planning", group: "Professionals", icon: "ruler", description: "Get the plan before the dust." },
  { id: "build", name: "Build & labour", group: "Professionals", icon: "hammer", description: "People who know the work." },
];

function categoryIcon(value: string) {
  if (value.includes("hammer") || value.includes("build")) return <Hammer className="size-5" />;
  if (value.includes("ruler") || value.includes("design")) return <Compass className="size-5" />;
  if (value.includes("grid") || value.includes("finish")) return <Store className="size-5" />;
  return <Wrench className="size-5" />;
}

function Home() {
  const [, setLocation] = useLocation();
  const categoriesQuery = useListCategories();
  const prosQuery = useListFeaturedProfessionals();
  const productsQuery = useListFeaturedProducts();
  const statsQuery = useGetDiscoveryStats();
  const categories = categoriesQuery.data ?? fallbackCategories;
  const pros = prosQuery.data ?? [];
  const products = productsQuery.data ?? [];
  const stats = statsQuery.data;
  const [search, setSearch] = useState("");
  const [kind, setKind] = useState("materials");
  const submitSearch = (event: FormEvent) => {
    event.preventDefault();
    const destination = kind === "professionals" ? "/professionals" : "/marketplace";
    setLocation(`${destination}${search.trim() ? `?query=${encodeURIComponent(search.trim())}` : ""}`);
  };

  return <Shell>
    <section className="paper-grid relative overflow-hidden border-b border-border bg-[#ece9df]">
      <div className="absolute -right-16 top-12 size-64 rounded-full border-[30px] border-secondary/25 sm:right-16 sm:size-80" />
      <div className="absolute right-8 top-24 hidden size-44 rotate-12 border border-primary/15 sm:block" />
      <div className="mx-auto grid max-w-[1240px] gap-12 px-5 pb-16 pt-14 sm:px-8 sm:pt-20 lg:grid-cols-[1.05fr_.95fr] lg:items-center lg:gap-20 lg:pb-24">
        <div className="relative z-10 rise-in">
          <div className="inline-flex items-center gap-2 rounded-full border border-primary/15 bg-background/70 px-3 py-1.5 font-mono-ui text-[10px] font-bold uppercase tracking-[0.14em] text-muted-foreground"><span className="size-1.5 rounded-full bg-accent" /> Building in Pakistan, made clearer</div>
          <h1 className="mt-6 max-w-2xl font-display text-[clamp(3.4rem,9vw,7.2rem)] font-bold leading-[.88] tracking-[-0.075em] text-primary">Start with a<br /><span className="text-accent">better brief.</span></h1>
          <p className="mt-7 max-w-lg text-lg leading-7 text-primary/70 sm:text-xl">The practical place to find materials, meet reliable professionals, and take the next right step on your build.</p>
          <form onSubmit={submitSearch} className="mt-8 max-w-2xl rounded-2xl border border-primary/15 bg-background p-2 shadow-[0_16px_40px_-24px_hsl(var(--primary)/.5)]" data-testid="form-home-search">
            <div className="flex flex-col gap-2 sm:flex-row">
              <div className="relative flex min-h-12 flex-1 items-center">
                <Search className="ml-3 size-5 text-muted-foreground" />
                <input value={search} onChange={(event) => setSearch(event.target.value)} placeholder="What are you building?" data-testid="input-home-search" className="h-12 w-full bg-transparent px-3 text-sm outline-none placeholder:text-muted-foreground" />
              </div>
              <label className="sr-only" htmlFor="home-search-kind">Search type</label>
              <select id="home-search-kind" value={kind} onChange={(event) => setKind(event.target.value)} data-testid="select-home-search-kind" className="h-12 rounded-xl border border-border bg-muted px-3 text-sm font-semibold outline-none">
                <option value="materials">Materials</option><option value="professionals">Professionals</option>
              </select>
              <button type="submit" data-testid="button-home-search" className="inline-flex h-12 items-center justify-center gap-2 rounded-xl bg-primary px-5 text-sm font-bold text-primary-foreground transition-transform hover:-translate-y-0.5">Search <ArrowRight className="size-4" /></button>
            </div>
          </form>
          <div className="mt-4 flex flex-wrap items-center gap-x-4 gap-y-2 text-xs font-medium text-muted-foreground"><span>Try:</span><button type="button" onClick={() => { setSearch("tiles"); setKind("materials"); }} data-testid="button-try-tiles" className="underline decoration-secondary decoration-2 underline-offset-4 hover:text-primary">tiles in Lahore</button><button type="button" onClick={() => { setSearch("architect"); setKind("professionals"); }} data-testid="button-try-architect" className="underline decoration-secondary decoration-2 underline-offset-4 hover:text-primary">an architect in Islamabad</button></div>
        </div>
        <div className="relative hidden min-h-[410px] lg:block rise-in-delay">
          <div className="absolute left-12 top-4 h-72 w-72 rotate-[-7deg] border-2 border-primary/20" />
          <div className="absolute left-24 top-16 h-72 w-72 rotate-[8deg] bg-primary p-5 text-primary-foreground shadow-2xl">
            <div className="flex items-center justify-between"><span className="font-mono-ui text-[10px] uppercase tracking-[.18em] text-secondary">Field note / 01</span><ArrowDownRight className="size-5 text-secondary" /></div>
            <div className="mt-28"><p className="font-display text-4xl font-bold leading-none tracking-[-.06em]">Good builds<br />start local.</p><p className="mt-4 max-w-[170px] text-xs leading-5 text-primary-foreground/60">Compare the people and products around you before the first site visit.</p></div>
            <div className="absolute bottom-5 left-5 right-5 border-t border-primary-foreground/15 pt-3 font-mono-ui text-[9px] uppercase tracking-[.15em] text-primary-foreground/45">Lahore · Karachi · Islamabad</div>
          </div>
        </div>
      </div>
    </section>

    <section className="mx-auto max-w-[1240px] px-5 py-16 sm:px-8 md:py-24">
      <div className="flex flex-col justify-between gap-5 md:flex-row md:items-end"><div><SectionLabel>What you can do here</SectionLabel><h2 className="mt-3 max-w-xl font-display text-4xl font-bold leading-[.98] tracking-[-.06em] sm:text-5xl">One desk.<br />Fewer loose ends.</h2></div><p className="max-w-sm text-sm leading-6 text-muted-foreground">Construction is a lot of small decisions. Buildora gives each one a sensible starting point.</p></div>
      <div className="mt-10 grid gap-4 md:grid-cols-3">
        <Link href="/marketplace" data-testid="card-action-marketplace" className="group rounded-2xl bg-primary p-6 text-primary-foreground transition-transform hover:-translate-y-1 sm:p-8"><Store className="size-7 text-secondary" /><h3 className="mt-16 font-display text-3xl font-bold tracking-[-.05em]">Find materials</h3><p className="mt-3 text-sm leading-6 text-primary-foreground/65">See what suppliers and shops are worth a call, starting with a short list.</p><span className="mt-7 inline-flex items-center gap-2 text-sm font-bold text-secondary">Browse marketplace <ArrowRight className="size-4 transition-transform group-hover:translate-x-1" /></span></Link>
        <Link href="/professionals" data-testid="card-action-professionals" className="group rounded-2xl border border-border bg-card p-6 transition-transform hover:-translate-y-1 sm:p-8"><Users className="size-7 text-accent" /><h3 className="mt-16 font-display text-3xl font-bold tracking-[-.05em]">Meet the right people</h3><p className="mt-3 text-sm leading-6 text-muted-foreground">Browse local architects, contractors and specialists with useful context.</p><span className="mt-7 inline-flex items-center gap-2 text-sm font-bold text-foreground">Explore professionals <ArrowRight className="size-4 transition-transform group-hover:translate-x-1" /></span></Link>
        <Link href="/projects" data-testid="card-action-projects" className="group rounded-2xl border border-border bg-[#dfe9e5] p-6 transition-transform hover:-translate-y-1 sm:p-8"><MessageSquareText className="size-7 text-primary" /><h3 className="mt-16 font-display text-3xl font-bold tracking-[-.05em]">Put out a brief</h3><p className="mt-3 text-sm leading-6 text-primary/65">Describe what you need and get your project thinking in one place.</p><span className="mt-7 inline-flex items-center gap-2 text-sm font-bold text-primary">Start a project brief <ArrowRight className="size-4 transition-transform group-hover:translate-x-1" /></span></Link>
      </div>
    </section>

    <section className="border-y border-border bg-card"><div className="mx-auto grid max-w-[1240px] gap-8 px-5 py-12 sm:px-8 md:grid-cols-[.85fr_1.15fr] md:items-center md:py-16"><div><SectionLabel>Early view</SectionLabel><h2 className="mt-3 font-display text-3xl font-bold leading-tight tracking-[-.05em]">A small, useful start.<br />Growing with the work.</h2><p className="mt-4 max-w-sm text-sm leading-6 text-muted-foreground">We are building this with local context in mind, not a one-size-fits-all directory.</p></div><div className="grid grid-cols-3 divide-x divide-border rounded-2xl border border-border bg-background"><Stat value={stats?.marketplaceItems} fallback="24" label="material leads" /><Stat value={stats?.professionalCategories} fallback="8" label="trade categories" /><Stat value={stats?.citiesCovered} fallback="3" label="cities covered" /></div></div></section>

    <section className="mx-auto max-w-[1240px] px-5 py-16 sm:px-8 md:py-24"><div className="flex items-end justify-between gap-4"><div><SectionLabel>From the desk</SectionLabel><h2 className="mt-3 font-display text-4xl font-bold tracking-[-.06em] sm:text-5xl">A few places to begin</h2></div><Link href="/marketplace" data-testid="link-home-see-all-products" className="hidden items-center gap-2 text-sm font-bold sm:inline-flex">See all materials <ArrowRight className="size-4" /></Link></div>{productsQuery.isLoading ? <div className="mt-8"><LoadingCards count={3} /></div> : productsQuery.isError ? <div className="mt-8"><QueryError onRetry={() => productsQuery.refetch()} /></div> : <div className="mt-8 grid gap-4 md:grid-cols-3">{products.slice(0, 3).map((product) => <ProductCard key={product.id} product={product} />)}{products.length === 0 && <EmptyState label="Material listings are being prepared." href="/marketplace" />}</div>}</section>

    <section className="bg-secondary"><div className="mx-auto flex max-w-[1240px] flex-col gap-6 px-5 py-12 sm:px-8 md:flex-row md:items-center md:justify-between md:py-14"><div><p className="font-mono-ui text-[10px] font-bold uppercase tracking-[.18em] text-primary/60">Not sure where to start?</p><h2 className="mt-2 max-w-xl font-display text-3xl font-bold leading-tight tracking-[-.05em] text-primary sm:text-4xl">Bring us the messy version.</h2></div><Link href="/assistant" data-testid="link-home-assistant" className="inline-flex w-fit items-center gap-2 rounded-xl bg-primary px-5 py-3 text-sm font-bold text-primary-foreground transition-transform hover:-translate-y-0.5">Open the assistant preview <ArrowRight className="size-4" /></Link></div></section>
  </Shell>;
}

function Stat({ value, fallback, label }: { value?: number; fallback: string; label: string }) {
  return <div className="px-3 py-5 text-center sm:px-6"><p className="font-display text-3xl font-bold tracking-[-.06em] text-primary sm:text-4xl">{value ?? fallback}</p><p className="mt-1 font-mono-ui text-[9px] uppercase leading-4 tracking-[.1em] text-muted-foreground">{label}</p></div>;
}

function ProductCard({ product }: { product: ProductPreview }) {
  return <article data-testid={`card-product-${product.id}`} className="group relative flex min-h-[220px] flex-col justify-between overflow-hidden rounded-2xl border border-border bg-card p-6 transition-transform hover:-translate-y-1"><div><div className="flex items-start justify-between gap-4"><span className="rounded-full bg-muted px-2.5 py-1 font-mono-ui text-[9px] font-bold uppercase tracking-[.12em] text-muted-foreground">{product.category}</span><DemoTag /></div><h3 className="mt-6 font-display text-2xl font-bold leading-tight tracking-[-.04em]">{product.name}</h3><p className="mt-2 line-clamp-2 text-sm leading-5 text-muted-foreground">{product.description}</p></div><div className="mt-6 flex items-end justify-between gap-3 border-t border-border pt-4"><div><p className="text-sm font-bold">{product.priceLabel}</p><p className="mt-1 flex items-center gap-1 text-xs text-muted-foreground"><MapPin className="size-3.5" /> {product.city} · {product.supplier}</p></div><ArrowUpRightIcon /></div></article>;
}
function ArrowUpRightIcon() { return <ArrowRight className="size-4 rotate-[-45deg] text-accent transition-transform group-hover:translate-x-1" />; }
function EmptyState({ label, href }: { label: string; href: string }) { return <div className="col-span-full rounded-2xl border border-dashed border-border bg-card p-10 text-center"><p className="font-semibold">{label}</p><Link href={href} data-testid="link-empty-state" className="mt-3 inline-flex items-center gap-2 text-sm font-bold text-accent">Explore the directory <ArrowRight className="size-4" /></Link></div>; }

function Marketplace() {
  const query = useListFeaturedProducts();
  const categoryQuery = useListCategories();
  const [term, setTerm] = useState("");
  const [category, setCategory] = useState("all");
  const products = query.data ?? [];
  const filtered = useMemo(() => products.filter((product) => (category === "all" || product.category === category) && `${product.name} ${product.supplier} ${product.city}`.toLowerCase().includes(term.toLowerCase())), [products, category, term]);
  const categories = [...new Set(products.map((item) => item.category))];
  return <Shell><PageIntro eyebrow="Marketplace / material desk" title="Know what is nearby before you buy." body="A practical shortlist of materials and suppliers to help you compare the next call, visit, or quote. Listing details are currently demo content while we build local coverage." /><section className="mx-auto max-w-[1240px] px-5 pb-20 sm:px-8"><div className="mb-8 flex flex-col gap-3 rounded-2xl border border-border bg-card p-3 sm:flex-row"><div className="relative flex flex-1 items-center"><Search className="ml-3 size-4 text-muted-foreground" /><input value={term} onChange={(event) => setTerm(event.target.value)} placeholder="Search products, suppliers or cities" data-testid="input-marketplace-search" className="h-11 w-full bg-transparent px-3 text-sm outline-none" /></div><select value={category} onChange={(event) => setCategory(event.target.value)} data-testid="select-marketplace-category" className="h-11 rounded-xl border border-border bg-muted px-3 text-sm font-semibold outline-none"><option value="all">All material types</option>{categories.map((item) => <option key={item} value={item}>{item}</option>)}</select></div>{query.isLoading ? <LoadingCards count={6} /> : query.isError ? <QueryError onRetry={() => query.refetch()} /> : filtered.length ? <div className="grid gap-4 md:grid-cols-2 lg:grid-cols-3">{filtered.map((product) => <ProductCard key={product.id} product={product} />)}</div> : <EmptyState label="No material leads match that search yet." href="/projects" />}<p className="mt-8 text-center font-mono-ui text-[10px] uppercase tracking-[.14em] text-muted-foreground">{categoryQuery.isLoading ? "Checking categories…" : `${filtered.length} listings in the early directory`}</p></section></Shell>;
}

function ProfessionalCard({ professional }: { professional: ProfessionalPreview }) {
  return <article data-testid={`card-professional-${professional.id}`} className="rounded-2xl border border-border bg-card p-6 transition-transform hover:-translate-y-1"><div className="flex items-start justify-between gap-3"><div className="grid size-12 place-items-center rounded-xl bg-primary font-display text-xl font-bold text-secondary">{professional.name.split(" ").map((part) => part[0]).slice(0, 2).join("")}</div>{professional.verified && <span className="inline-flex items-center gap-1 rounded-full bg-[#dfe9e5] px-2.5 py-1 text-[10px] font-bold text-primary"><BadgeCheck className="size-3.5" /> Verified</span>}</div><h3 className="mt-5 font-display text-2xl font-bold tracking-[-.04em]">{professional.name}</h3><p className="mt-1 text-sm font-semibold text-accent">{professional.category}</p><p className="mt-4 line-clamp-2 text-sm leading-6 text-muted-foreground">{professional.description}</p><div className="mt-5 flex flex-wrap gap-3 border-t border-border pt-4 text-xs font-medium text-muted-foreground"><span className="inline-flex items-center gap-1"><MapPin className="size-3.5" />{professional.city}</span><span className="inline-flex items-center gap-1"><Star className="size-3.5 fill-secondary text-secondary" />{professional.rating.toFixed(1)}</span><span>{professional.yearsExperience} years in the work</span></div><Link href={`/projects?professional=${professional.id}`} data-testid={`link-contact-professional-${professional.id}`} className="mt-5 inline-flex items-center gap-2 text-sm font-bold text-primary">Ask about your project <ArrowRight className="size-4" /></Link></article>;
}

function Professionals() {
  const query = useListFeaturedProfessionals();
  const [term, setTerm] = useState("");
  const pros = query.data ?? [];
  const filtered = pros.filter((person) => `${person.name} ${person.category} ${person.city}`.toLowerCase().includes(term.toLowerCase()));
  return <Shell><PageIntro eyebrow="Professionals / local network" title="The people behind the good work." body="Explore a starting list of architects, builders and specialists. Read the context, then make the call that feels right. Profiles shown here are demo content." /><section className="mx-auto max-w-[1240px] px-5 pb-20 sm:px-8"><div className="mb-8 flex items-center rounded-2xl border border-border bg-card p-3"><Search className="ml-3 size-4 text-muted-foreground" /><input value={term} onChange={(event) => setTerm(event.target.value)} placeholder="Search by name, trade or city" data-testid="input-professionals-search" className="h-11 w-full bg-transparent px-3 text-sm outline-none" /></div>{query.isLoading ? <LoadingCards count={6} /> : query.isError ? <QueryError onRetry={() => query.refetch()} /> : filtered.length ? <div className="grid gap-4 md:grid-cols-2 lg:grid-cols-3">{filtered.map((person) => <ProfessionalCard key={person.id} professional={person} />)}</div> : <EmptyState label="No professionals match that search yet." href="/projects" />}</section></Shell>;
}

function Projects() {
  const [sent, setSent] = useState(false);
  const [form, setForm] = useState({ name: "", city: "", type: "", details: "" });
  const update = (field: keyof typeof form) => (event: ChangeEvent<HTMLInputElement | HTMLTextAreaElement | HTMLSelectElement>) => setForm((current) => ({ ...current, [field]: event.target.value }));
  return <Shell><div className="mx-auto grid max-w-[1240px] gap-12 px-5 pb-20 pt-12 sm:px-8 md:grid-cols-[.8fr_1.2fr] md:items-start md:gap-20 md:pt-20"><div><SectionLabel>Project brief</SectionLabel><h1 className="mt-4 font-display text-5xl font-bold leading-[.94] tracking-[-.065em] sm:text-7xl">Put the<br /><span className="text-accent">messy bit</span><br />somewhere.</h1><p className="mt-6 max-w-sm text-base leading-7 text-muted-foreground">This is your private starting note. Tell us what you know, what you do not, and where the work is happening.</p><div className="mt-10 space-y-4 text-sm text-muted-foreground"><p className="flex gap-3"><Check className="mt-0.5 size-4 shrink-0 text-accent" />No perfect brief required.</p><p className="flex gap-3"><Check className="mt-0.5 size-4 shrink-0 text-accent" />We will keep the next step visible.</p><p className="flex gap-3"><Check className="mt-0.5 size-4 shrink-0 text-accent" />You can browse first, decide later.</p></div></div><div className="rounded-2xl border border-border bg-card p-5 shadow-sm sm:p-8">{sent ? <div className="flex min-h-[430px] flex-col items-center justify-center text-center"><div className="grid size-16 place-items-center rounded-2xl bg-secondary text-primary"><Check className="size-8" /></div><h2 className="mt-6 font-display text-3xl font-bold tracking-[-.05em]">Your brief is saved locally.</h2><p className="mt-3 max-w-sm text-sm leading-6 text-muted-foreground">The project-request connection is the next phase. For now, your note is ready to use as you compare people and materials.</p><Link href="/dashboard" data-testid="link-project-success-dashboard" className="mt-7 inline-flex items-center gap-2 rounded-xl bg-primary px-5 py-3 text-sm font-bold text-primary-foreground">View your planning desk <ArrowRight className="size-4" /></Link></div> : <form onSubmit={(event) => { event.preventDefault(); setSent(true); }} className="space-y-5" data-testid="form-project-brief"><div><label htmlFor="project-name" className="text-sm font-bold">Your name</label><input required id="project-name" value={form.name} onChange={update("name")} data-testid="input-project-name" className="mt-2 h-12 w-full rounded-xl border border-input bg-background px-4 text-sm outline-none focus:ring-2 focus:ring-secondary" placeholder="What should we call you?" /></div><div className="grid gap-5 sm:grid-cols-2"><div><label htmlFor="project-city" className="text-sm font-bold">Build city</label><input required id="project-city" value={form.city} onChange={update("city")} data-testid="input-project-city" className="mt-2 h-12 w-full rounded-xl border border-input bg-background px-4 text-sm outline-none focus:ring-2 focus:ring-secondary" placeholder="Lahore, Karachi…" /></div><div><label htmlFor="project-type" className="text-sm font-bold">What are you planning?</label><select required id="project-type" value={form.type} onChange={update("type")} data-testid="select-project-type" className="mt-2 h-12 w-full rounded-xl border border-input bg-background px-4 text-sm outline-none focus:ring-2 focus:ring-secondary"><option value="">Choose one</option><option>New home</option><option>Renovation</option><option>Commercial space</option><option>Something else</option></select></div></div><div><label htmlFor="project-details" className="text-sm font-bold">The rough version</label><textarea required id="project-details" value={form.details} onChange={update("details")} data-testid="textarea-project-details" className="mt-2 min-h-36 w-full resize-y rounded-xl border border-input bg-background px-4 py-3 text-sm leading-6 outline-none focus:ring-2 focus:ring-secondary" placeholder="What do you need help deciding?" /></div><button type="submit" data-testid="button-submit-project" className="inline-flex h-12 w-full items-center justify-center gap-2 rounded-xl bg-primary text-sm font-bold text-primary-foreground transition-transform hover:-translate-y-0.5">Save my project brief <ArrowRight className="size-4" /></button><p className="text-center font-mono-ui text-[9px] uppercase tracking-[.12em] text-muted-foreground">Demo only — nothing is sent yet</p></form>}</div></div></Shell>;
}

function Assistant() {
  const [question, setQuestion] = useState("");
  const [asked, setAsked] = useState(false);
  return <Shell><section className="mx-auto max-w-[1000px] px-5 py-12 sm:px-8 md:py-20"><div className="mx-auto max-w-2xl text-center"><div className="mx-auto grid size-16 place-items-center rounded-2xl bg-primary text-secondary"><Sparkles className="size-7" /></div><SectionLabel>Next phase / assistant</SectionLabel><h1 className="mt-4 font-display text-5xl font-bold leading-[.95] tracking-[-.07em] sm:text-7xl">Ask the question<br /><span className="text-accent">you are avoiding.</span></h1><p className="mt-6 text-base leading-7 text-muted-foreground">A construction guide that will help you make sense of quotes, materials and the order of things. We are still teaching it the local detail.</p></div><div className="mx-auto mt-12 max-w-2xl rounded-2xl border border-border bg-card p-4 sm:p-6"><div className="flex items-center gap-3 border-b border-border pb-4"><CircleHelp className="size-5 text-accent" /><span className="text-sm font-bold">What would you like to understand?</span></div><form onSubmit={(event) => { event.preventDefault(); setAsked(true); }} className="mt-4" data-testid="form-assistant-question"><textarea required value={question} onChange={(event) => setQuestion(event.target.value)} data-testid="textarea-assistant-question" className="min-h-32 w-full resize-none bg-transparent text-base leading-7 outline-none placeholder:text-muted-foreground" placeholder="For example: what should I ask a contractor before comparing quotes?" /><div className="mt-4 flex flex-col items-start justify-between gap-4 border-t border-border pt-4 sm:flex-row sm:items-center"><p className="font-mono-ui text-[9px] uppercase tracking-[.12em] text-muted-foreground">Preview mode · replies arrive next phase</p><button type="submit" data-testid="button-ask-assistant" className="inline-flex items-center gap-2 rounded-xl bg-primary px-5 py-3 text-sm font-bold text-primary-foreground">Send question <ArrowRight className="size-4" /></button></div></form>{asked && <div className="mt-5 rounded-xl bg-muted p-4 text-sm leading-6 text-muted-foreground" data-testid="text-assistant-response">Good question. Buildora will be able to break that into a practical checklist in the next phase. For now, try writing down the quote, the scope, and what is not included.</div>}</div></section></Shell>;
}

function CalculatorPage() {
  const [area, setArea] = useState("");
  const [type, setType] = useState("standard");
  const [estimate, setEstimate] = useState<number | null>(null);
  const rate = type === "premium" ? 7200 : type === "basic" ? 4200 : 5600;
  return <Shell><PageIntro eyebrow="Next phase / cost calculator" title="Put a first number on the napkin." body="A simple planning calculator is on the way. This preview lets you test the shape of it without pretending to be a quote." /><section className="mx-auto grid max-w-[1000px] gap-8 px-5 pb-20 sm:px-8 md:grid-cols-[1fr_.8fr]"><form onSubmit={(event) => { event.preventDefault(); setEstimate(Number(area) * rate); }} className="rounded-2xl border border-border bg-card p-6 sm:p-8" data-testid="form-calculator"><div><label htmlFor="calculator-area" className="text-sm font-bold">Approximate covered area (sq ft)</label><input required min="1" type="number" id="calculator-area" value={area} onChange={(event) => setArea(event.target.value)} data-testid="input-calculator-area" className="mt-2 h-12 w-full rounded-xl border border-input bg-background px-4 text-sm outline-none focus:ring-2 focus:ring-secondary" placeholder="e.g. 1,200" /></div><div className="mt-5"><label htmlFor="calculator-finish" className="text-sm font-bold">Finish level</label><select id="calculator-finish" value={type} onChange={(event) => setType(event.target.value)} data-testid="select-calculator-finish" className="mt-2 h-12 w-full rounded-xl border border-input bg-background px-4 text-sm outline-none"><option value="basic">Essential finish</option><option value="standard">Balanced finish</option><option value="premium">Higher finish</option></select></div><button type="submit" data-testid="button-calculate" className="mt-6 inline-flex h-12 w-full items-center justify-center gap-2 rounded-xl bg-primary text-sm font-bold text-primary-foreground">Show planning estimate <Calculator className="size-4" /></button></form><div className="rounded-2xl bg-primary p-6 text-primary-foreground sm:p-8"><Calculator className="size-7 text-secondary" /><p className="mt-16 font-mono-ui text-[10px] uppercase tracking-[.16em] text-primary-foreground/55">Early planning range</p><p className="mt-3 font-display text-4xl font-bold tracking-[-.06em]">{estimate ? `PKR ${estimate.toLocaleString()}` : "— — —"}</p><p className="mt-4 text-sm leading-6 text-primary-foreground/65">{estimate ? "A rough direction, not a contractor quote. Labour, land conditions and city rates will change the final number." : "Enter your area to see a deliberately rough starting point."}</p><DemoTag /></div></section></Shell>;
}

function About() {
  return <Shell><PageIntro eyebrow="About Buildora" title="A calmer way to begin building." body="The first decision is rarely the most expensive one. It is usually knowing who to ask, what to compare, and what comes next." /><section className="mx-auto grid max-w-[1240px] gap-12 px-5 pb-20 sm:px-8 md:grid-cols-[1.1fr_.9fr] md:gap-20"><div className="space-y-6 text-base leading-8 text-muted-foreground"><p>Buildora is a construction discovery platform for people in Pakistan who are planning a home, improving a space, or simply trying to make sense of the work ahead.</p><p>We are starting with the practical layer: materials worth exploring, professionals worth contacting, and a place to put your project brief. No inflated promises. Just clearer next steps.</p><p>Our directory begins with demo content so we can shape the experience before local listings become the centre of it. Every demo surface is marked clearly.</p></div><div className="rounded-2xl bg-primary p-7 text-primary-foreground sm:p-9"><SectionLabel>Our working rule</SectionLabel><p className="mt-8 font-display text-4xl font-bold leading-[.98] tracking-[-.06em]">Make the next decision easier than the last.</p><div className="mt-10 border-t border-primary-foreground/15 pt-5 text-sm leading-6 text-primary-foreground/65">Built for local context, careful comparisons, and the long middle between idea and handover.</div></div></section></Shell>;
}

function AuthPage({ mode }: { mode: "sign-in" | "sign-up" }) {
  const [, setLocation] = useLocation();
  const [submitted, setSubmitted] = useState(false);
  const isSignIn = mode === "sign-in";
  return <div className="grain flex min-h-[100dvh] bg-[#ece9df]"><div className="hidden w-[42%] flex-col justify-between bg-primary p-10 text-primary-foreground lg:flex"><div><Link href="/" data-testid="link-auth-logo" className="inline-flex"><span className="font-display text-3xl font-bold tracking-[-.06em]">buildora</span></Link><div className="mt-28 max-w-sm"><SectionLabel>Welcome to the desk</SectionLabel><h1 className="mt-4 font-display text-6xl font-bold leading-[.9] tracking-[-.07em]">Good builds<br /><span className="text-secondary">begin clearly.</span></h1><p className="mt-6 text-sm leading-6 text-primary-foreground/60">Save your briefs, keep your planning in one place, and return when you are ready for the next call.</p></div></div><p className="font-mono-ui text-[10px] uppercase tracking-[.15em] text-primary-foreground/45">Pakistan / early access</p></div><div className="flex flex-1 items-center justify-center px-5 py-10 sm:px-8"><div className="w-full max-w-[420px]"><div className="mb-10 lg:hidden"><Link href="/" data-testid="link-auth-mobile-logo" className="font-display text-3xl font-bold tracking-[-.06em]">buildora</Link></div>{submitted ? <div className="rounded-2xl border border-border bg-card p-7 text-center sm:p-9"><div className="mx-auto grid size-14 place-items-center rounded-2xl bg-secondary text-primary"><Check className="size-7" /></div><h2 className="mt-5 font-display text-3xl font-bold tracking-[-.05em]">Demo access noted.</h2><p className="mt-3 text-sm leading-6 text-muted-foreground">Clerk authentication will take over this branded screen when the auth wiring is connected.</p><Link href="/" data-testid="link-auth-success-home" className="mt-6 inline-flex items-center gap-2 font-bold">Back to Buildora <ArrowRight className="size-4" /></Link></div> : <form onSubmit={(event) => { event.preventDefault(); setSubmitted(true); }} className="rounded-2xl border border-border bg-card p-7 sm:p-9" data-testid={`form-${mode}`}><SectionLabel>{isSignIn ? "Return to your desk" : "Make a place for your plans"}</SectionLabel><h1 className="mt-4 font-display text-4xl font-bold tracking-[-.06em]">{isSignIn ? "Welcome back." : "Create your Buildora account."}</h1><p className="mt-3 text-sm leading-6 text-muted-foreground">{isSignIn ? "Keep your project thinking close." : "Save your briefs and pick up where you left off."}</p><label htmlFor="auth-email" className="mt-8 block text-sm font-bold">Email address</label><input required type="email" id="auth-email" data-testid="input-auth-email" className="mt-2 h-12 w-full rounded-xl border border-input bg-background px-4 text-sm outline-none focus:ring-2 focus:ring-secondary" placeholder="you@example.com" /><label htmlFor="auth-password" className="mt-5 block text-sm font-bold">Password</label><input required minLength={6} type="password" id="auth-password" data-testid="input-auth-password" className="mt-2 h-12 w-full rounded-xl border border-input bg-background px-4 text-sm outline-none focus:ring-2 focus:ring-secondary" placeholder="At least 6 characters" /><button type="submit" data-testid={`button-submit-${mode}`} className="mt-6 h-12 w-full rounded-xl bg-primary text-sm font-bold text-primary-foreground">{isSignIn ? "Sign in" : "Create account"}</button><p className="mt-6 text-center text-sm text-muted-foreground">{isSignIn ? "New to Buildora?" : "Already have an account?"} <button type="button" onClick={() => setLocation(isSignIn ? "/sign-up" : "/sign-in")} data-testid="button-switch-auth" className="font-bold text-accent underline underline-offset-4">{isSignIn ? "Create one" : "Sign in"}</button></p></form>}</div></div></div>;
}

function Dashboard() {
  return <Shell><div className="mx-auto max-w-[1240px] px-5 py-12 sm:px-8 md:py-20"><SectionLabel>Your planning desk</SectionLabel><div className="mt-4 flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><h1 className="font-display text-5xl font-bold tracking-[-.07em] sm:text-6xl">Good morning.</h1><p className="mt-3 text-muted-foreground">A quiet place to keep the next decision visible.</p></div><Link href="/projects" data-testid="link-dashboard-new-project" className="inline-flex w-fit items-center gap-2 rounded-xl bg-primary px-5 py-3 text-sm font-bold text-primary-foreground">New project brief <ArrowRight className="size-4" /></Link></div><div className="mt-12 grid gap-4 md:grid-cols-[1.1fr_.9fr]"><div className="rounded-2xl bg-primary p-7 text-primary-foreground sm:p-9"><p className="font-mono-ui text-[10px] uppercase tracking-[.16em] text-secondary">Your next step</p><h2 className="mt-5 max-w-md font-display text-4xl font-bold leading-[.98] tracking-[-.06em]">Start with what you know.</h2><p className="mt-4 max-w-sm text-sm leading-6 text-primary-foreground/60">Your saved briefs, material comparisons and professional notes will live here once your account is connected.</p><Link href="/projects" data-testid="link-dashboard-start-brief" className="mt-8 inline-flex items-center gap-2 text-sm font-bold text-secondary">Write a rough brief <ArrowRight className="size-4" /></Link></div><div className="rounded-2xl border border-border bg-card p-7 sm:p-9"><p className="font-mono-ui text-[10px] uppercase tracking-[.16em] text-accent">Your workspace</p><div className="mt-7 space-y-4"><DashboardRow icon={<MessageSquareText />} label="Project briefs" value="Ready for your first" href="/projects" /><DashboardRow icon={<Store />} label="Saved materials" value="Browse the marketplace" href="/marketplace" /><DashboardRow icon={<Users />} label="People to call" value="Explore professionals" href="/professionals" /></div></div></div></div></Shell>;
}
function DashboardRow({ icon, label, value, href }: { icon: ReactNode; label: string; value: string; href: string }) { return <Link href={href} data-testid={`link-dashboard-${label.toLowerCase().replaceAll(" ", "-")}`} className="flex items-center gap-3 rounded-xl border border-border p-3 transition-colors hover:bg-muted"><span className="grid size-9 place-items-center rounded-lg bg-muted text-accent">{icon}</span><span className="flex-1"><span className="block text-sm font-bold">{label}</span><span className="block text-xs text-muted-foreground">{value}</span></span><ChevronDown className="size-4 rotate-[-90deg] text-muted-foreground" /></Link>; }

export { About, Assistant, AuthPage, CalculatorPage, Dashboard, Home, Marketplace, Professionals, Projects };